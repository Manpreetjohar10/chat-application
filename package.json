{
  "name": "chatboat",
  "version": "1.0.0",
  "description": "Realtime chat app using Firebase Realtime Database",
  "private": true,
  "scripts": {
    "build:config": "node scripts/generate-firebase-config.js"
  }
}
